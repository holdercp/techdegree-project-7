ul.app--user--list
  each friend in friends
    li
      .circle--fluid
        .circle--fluid--cell.circle--fluid--primary
          a.app--tweet--author
            .app--avatar(style=`background-image: url(${friend.imageProf})`)
              img(src=friend.imageProf)
            h4= friend.name
            p= `@${friend.handle}`
        .circle--fluid--cell
          a.button.button-text Unfollow
